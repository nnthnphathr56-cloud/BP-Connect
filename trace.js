const fs=require('fs');const file=fs.readdirSync('.').find(f=>f.endsWith('.html'));const lines=fs.readFileSync(file,'utf8').split('\n');let depth=0;for(let i=2160;i<3000;i++){const open=(lines[i].match(/<div/g)||[]).length;const close=(lines[i].match(/<\/div>/g)||[]).length;depth+=open-close;if(lines[i].includes('class="citizen-subpage'))console.log((i+1)+': subpage depth before: '+(depth-open+close));}
