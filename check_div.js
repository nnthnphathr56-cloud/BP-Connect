const fs=require('fs');const html=fs.readFileSync('d:/bpconnect/ระบบรับแจ้งปัญหา.html','utf8');let line=1;let stack=[];const regex=/<div[^>]*>|<\/div>/gi;let match;while((match=regex.exec(html))!==null){line=html.substring(0,match.index).split('\n').length;if(match[0].toLowerCase()==='</div>'){stack.pop();}else{let idMatch=match[0].match(/id=[\"']([^\"']+)[\"']/);let id=idMatch?idMatch[1]:'';stack.push({line,tag:match[0],id});}}console.log('Unclosed divs:',stack);
